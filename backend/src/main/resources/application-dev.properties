# ============================================
# Profil DEVELOPPEMENT LOCAL
# ============================================

# MySQL LOCAL (Docker avec port 3306 expose)
spring.datasource.url=jdbc:mysql://localhost:3306/orisha_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=orisha_user
spring.datasource.password=DevPass123!

# JPA / Hibernate - Mode developpement
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# SQL initialization (charger data.sql si present)
spring.sql.init.mode=always